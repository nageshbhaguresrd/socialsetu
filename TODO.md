# TODO

- [x] Identify conflicting API handlers: app/api/leads/[id]/page.tsx vs app/api/leads/[id]/route.ts
- [x] Remove API page.tsx (conflict) -> keep app/api/leads/[id]/route.ts
- [x] Ensure api endpoints work (build) via `npm run build`
